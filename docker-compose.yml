version: "3.9"

services:
  api:
    image: ${DOCKER_IMAGE:-mohsin1920/docker-cicd-learning}:${IMAGE_TAG:-latest}

    container_name: docker-cicd-learning

    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env
